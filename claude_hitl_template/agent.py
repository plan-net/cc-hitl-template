"""
Minimal placeholder agent logic for Claude + Kodosumi HITL template.

This module is intentionally kept minimal. Expand with your own business logic and tools.
"""

def process_message(message: str) -> str:
    """
    Placeholder function to demonstrate where business logic goes.

    TODO: Replace with actual agent logic and tools as needed.
    Example extensions:
    - Add API calls to external services
    - Implement data processing logic
    - Create custom tools for Claude Agent SDK
    - Add validation and error handling

    Args:
        message: User input message

    Returns:
        Processed response (currently just echoes input)
    """
    # Minimal placeholder - just echo the message
    return f"Received: {message}"
